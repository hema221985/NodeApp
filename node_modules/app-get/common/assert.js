'use strict';

const logger = require('./logger');

const must = (judge, text) => {
  if (!judge) {
    logger.info(text);
    process.exit(0)
  }
};

const should = (judge, text) => {
  if (!judge) {
    logger.info(text);
  }
};

module.exports = {
  must,
  should
};
