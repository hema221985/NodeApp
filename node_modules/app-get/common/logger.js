'use strict';

const debug = console.log;
const info = console.log;
const err = console.error;
const warn = console.warn;

const issue = () => {
  info(`\nIt will be appreciated if you report this to http://github.com/yuhai1023/app-get/issues`)
};

module.exports = {
  debug, info, warn, err, issue
};
